[tool.ruff]
target-version = "py310"
line-length = 120
exclude = [
    ".venv",
    "migrations",
    "manual_test_wallet_service.py",
]

[tool.ruff.lint]
select = ["E", "F", "W"]
ignore = [
    "E501",    # Line length handled gracefully
]
